---
- hosts: gateway
  gather_facts: no
  strategy: linear
  roles:
    - sg-ansible-setup

- hosts: gateway
  strategy: linear
  become: yes
  become_method: doas
  roles:
    - sg-base
    - sg-dhcp
    - sg-dns
    - sg-ntp
    - sg-pxeboot

{
    "cniVersion": "0.4.0",
    "name": "{{item.name}}",
    "plugins": [
        {
            "type": "macvlan",
            "master": "{{item.parent}}",
            "ipam": {
                "type": "dhcp"
            }
        }
    ]
}
